# kaikai-ig-assets

Public image assets for `@kaikai_blog` Instagram publishing.

This repo is used as the origin for jsDelivr URLs that are later passed into the Instagram Graph API.

Typical path pattern:

- `images/<slug>.png`
- `images/<slug>.jpg`
